import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './register/App';

const root = ReactDOM.createRoot(document.getElementById('umsiri'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);